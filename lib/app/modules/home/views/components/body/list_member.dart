import 'package:flutter/material.dart';

class ListMemeber extends StatelessWidget {
  const ListMemeber({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Danh sach thanh vien'),
    );
  }
}
